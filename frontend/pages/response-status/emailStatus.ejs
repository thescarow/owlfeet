<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, minimum-scale=1.0, maximum-scale = 1.0, user-scalable = no">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">

    <link rel="apple-touch-icon" sizes="180x180" href="/assets/favicon/apple-touch-icon.png">
    <link rel="icon" type="image/png" sizes="32x32" href="/assets/favicon/favicon-32x32.png">
    <link rel="icon" type="image/png" sizes="16x16" href="/assets/favicon/favicon-16x16.png">
    <link rel="manifest" href="/assets/favicon/site.webmanifest">



    <link rel="stylesheet" href="/stylesheets/response-status/emailStatus.css" />
</head>

<body>

    <div class="main-status-container" id='mainStatusContainer'>

        <div class='status-box <% if(locals.status==="email-verified" ){ %>
        status-box--email-verified
        <% } else if(locals.status==="failed" ){ %>
        status-box--failed
        <% } _%>'>
            <div class="status__icon">
                <% if(locals.status==='email-verified' ){ %>
                    <%- include('../partials/icons/extra/emailVerifiedIcon.svg') _%>
                        <% } else if(locals.status==='failed' ){ %>
                            <%- include('../partials/icons/extra/failedIcon.svg') _%>
                                <% } _%>
            </div>
            <div class="status__title">
                <% if(locals.status==='email-verified' ){ %>
                    Verified
                    <% } else if(locals.status==='failed' ){ %>
                        <%= locals.errorTitle _%>
                            <% } _%>
            </div>
            <div class="status__desc">
                <% if(locals.status==='email-verified' ){ %>
                    Your email has been verified.
                    <% } else if(locals.status==='failed' ){ %>
                        <%= locals.errorDesc _%>
                            <% } _%>
            </div>

            <div class="status__btn-field">
                <div class="status__btn">

                </div>
            </div>

        </div>
    </div>
</body>

</html>